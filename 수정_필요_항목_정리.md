# 프로토타입 수정 필요 항목 정리 (2026-08-06)

## 업데이트 (같은 날 후속 작업)
A-1, A-2, B-1, C-1, C-2는 관리자 목업 5탭을 삭제하고 "회차관리(실제연동)" 안에 이미 있던 실제 API 서브탭 6개(회차관리/성적표/학생조회/대시보드/문항통계/대학DB)를 최상단으로 승격시키면서 전부 해결됨. 파일 용량도 999KB → 66.5KB로 줄어듦 (861KB 목업 데이터 블록 및 관련 코드 약 830줄 삭제). 아래 A/B 섹션은 처리 완료 기록으로 남겨두고, C-3/C-4만 남은 작업임.

## A. 이미 파악하고 있던, 나중으로 미룬 이슈 — ✅ 해결됨

### A-1. 관리자 탭 5개가 아직 실제 API에 연동되지 않음
관리자 모드 상단 탭은 6개: **대시보드 / 학생 관리 / 문항 통계 / 성적표 / 대학 DB / 회차관리(실제연동)**.

이 중 마지막 "회차관리(실제연동)" 탭만 실제 배포된 백엔드(API_BASE)와 통신한다. 나머지 5개는 파일 안에 하드코딩된 예시 데이터(2024년 정병권P 반, `DATA` 변수, 약 861KB)를 그대로 보여준다. 코드 주석에도 명시되어 있음:

> "위의 다른 탭들은 전부 이 파일 안의 예시 데이터(DATA/OMR_FILES/ROSTER_FILES)로 동작하는 프로토타입입니다."

즉 관리자가 "성적표"나 "대시보드" 탭에서 보는 내용은 실제 업로드한 회차 데이터가 아니라 예전 프로토타입 데모 데이터다. **나중에 수정해도 된다고 이미 합의된 사항.**

### A-2. 위 이슈와 연결된 861KB 하드코딩 데이터 블록
`var DATA = {...}` (파일 118번째 줄, 약 861KB, 전체 파일 용량의 85%)에서 파생된 `STUDENTS`/`META`/`UNIV_MASTER`/`ALL_UNIS` 전역변수가 A-1의 5개 탭 및 `studentsForFile()`(파일 미리보기 기능)에서 계속 쓰이고 있다. A-1이 실제 API 연동으로 전환되면 이 블록도 통째로 제거 가능 — 파일 용량이 크게 줄어든다 (현재 1MB → 약 150KB 예상).

## B. 오늘 목업 탭 제거 작업 중 발견한 부수 이슈 — ✅ 해결됨

### B-1. `발행`/`발행취소` 토글이 고아 기능이 됨
`state.published` 및 `publish`/`unpublish` 액션은 원래 구버전 "학생 포털" 목업 탭의 잠금을 해제하는 용도였다. 오늘 그 구버전 탭을 완전히 제거하면서, 이제 이 토글은 대시보드의 "성적표 발행 완료 / 미발행" 상태 배지 표시 외에는 아무 기능도 하지 않는다. 제거하거나, 다른 의미로 재활용할지 결정 필요.

## C. 기존 갭 분석에서 낮은 우선순위로 남겨둔 항목 (재확인 결과)

### C-1. 대시보드 히스토그램 / 계열분포 차트 — ✅ 해결됨 (해당 없음)
목업 대시보드 자체가 삭제되어 이 항목은 더 이상 유효하지 않음. 실제연동 대시보드(`renderLiveDashboardTab`)에 이런 차트를 추가하고 싶다면 별도 신규 기능 요청으로 다시 논의.

### C-2. 학생 전체검색 — ✅ 해결됨
실제연동 학생조회 탭(`renderLiveStudentsTab`)이 실제 DB 기준으로 동작 — 목업 문제가 없어지면서 함께 해결됨.

### C-3. 대학DB 개별수정 폼 — 미구현 확인 (남은 작업)
실제연동 "대학DB" 탭(`renderLiveUnivTab`)도 조회 위주로 보임. 백엔드에는 대량업로드 API(`/api/university-master/bulk-upload`)만 있고, 개별 대학/학과 단위로 수정하는 API·UI는 없음.

### C-4. 대학별 성적표 변형 — 미구현으로 재확인 (남은 작업)
예전에 있던 대학별 전환 탭(univ-tabs) UI는 목업 성적표(`renderReportCard`)에만 있던 것으로, 오늘 그 함수 자체가 삭제됨. 실제연동 성적표(`renderLiveResultsTab`)에는 대학별 변형 UI가 아예 없음. 필요하면 신규로 추가해야 함.

---

## 남은 작업
없음 — C-3, C-4 모두 구현 완료 (아래 참고)

### C-3 구현 내용
백엔드(`src/routes/universityMaster.js`)에 `PUT /:id`, `DELETE /:id` 추가. "대학 DB" 탭에 개별 추가 폼 + 각 행 수정/삭제 버튼 추가.

### C-4 구현 내용
"성적표" 탭의 학생별 결과 행마다 "매칭보기" 버튼 추가. 클릭하면 해당 학생의 희망학과 기준으로 대학 마스터 DB와 대조한 매칭 테이블(학과 내 석차, 모집인원 대비 판단)을 보여줌. 학생 포털(실제)에 이미 있던 `renderPvUnivTab`과 동일한 방식으로 구현해 스타일/로직 일관성 유지.
